# Budget App

Personal budgeting app with:
- Flask backend
- React frontend
- PostgreSQL (cloud)

## Local Setup

### Backend
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python run.py

### Frontend
cd frontend
npm install
npm run dev
